---
layout: none
---

[
  {% for post in site.posts %}
    {
    "title": "{{ post.title }}",
    "url": "{{ post.url }}", 
    "description": "{{ post.description }}",
    "image": "{{ post.image }}",
    "media": "{{ post.media }}",
    "date": "{{ post.date }}"   
    }{% unless forloop.last %},{% endunless %}
  {% endfor %}
]
