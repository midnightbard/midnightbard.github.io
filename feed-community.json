---
layout: none
permalink: /feed-community.json
---

[
  {% for post in site.community %}
    {
      "title": {{ post.title | jsonify }},
      "url": "{{ site.url }}{{ post.url }}",
      "description": {{ post.description | jsonify }},
      "image": "{{ post.image }}",
      "media": "{{ post.media }}",
      "date": "{{ post.date | date_to_xmlschema }}"
    }{% unless forloop.last %},{% endunless %}
  {% endfor %}
]
